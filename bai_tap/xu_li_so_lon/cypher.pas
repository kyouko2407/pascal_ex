var t, n, count_u, count_d,k,i,j,m : longint;
s : string
a :array[1..100] of longint 
begin
    readln(t);
    for i := 1 to t do 
        begin
            readln(n);
            for j := 1 to n do read(a[i]);
            
        end;
end.
